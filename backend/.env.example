DEBUG=True
SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173

# MySQL connection (matches docs/02_PLAN.md "PostgreSQL/production-ready option",
# adapted here to the MySQL instance used for local development).
DB_ENGINE=mysql
DB_NAME=python_album_catalog
DB_USER=mysql
DB_PASSWORD=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
